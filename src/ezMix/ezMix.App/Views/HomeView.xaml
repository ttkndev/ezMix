<UserControl x:Class="ezMix.App.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Style="{StaticResource PageContentGridStyle}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock Text="{Binding Title}" Style="{StaticResource PageTitleTextStyle}" Margin="0,0,0,12"/>
                <TextBlock Text="{Binding Description}" Style="{StaticResource BodyTextStyle}" TextWrapping="Wrap" Margin="0,0,0,20"/>
            </StackPanel>

            <Border Grid.Row="1" Style="{StaticResource CardPanelStyle}" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Năng lực nổi bật" Style="{StaticResource SectionTitleTextStyle}" Margin="0,0,0,12"/>
                    <ItemsControl ItemsSource="{Binding Highlights}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding StringFormat=• {0}}" Style="{StaticResource BodyTextStyle}" Margin="0,4,0,4" TextWrapping="Wrap"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <WrapPanel Grid.Row="2" ItemWidth="110" Orientation="Horizontal">
                <Button Content="Primary" Style="{StaticResource BtnPrimary}"/>
                <Button Content="Secondary" Style="{StaticResource BtnSecondary}"/>
                <Button Content="Success" Style="{StaticResource BtnSuccess}"/>
                <Button Content="Danger" Style="{StaticResource BtnDanger}"/>
                <Button Content="Warning" Style="{StaticResource BtnWarning}"/>
                <Button Content="Info" Style="{StaticResource BtnInfo}"/>
            </WrapPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
