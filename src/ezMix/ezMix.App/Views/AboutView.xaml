<UserControl x:Class="ezMix.App.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{DynamicResource Brush.Background.App}">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="40,28,40,40" MaxWidth="1000" HorizontalAlignment="Center">
                <!-- Header section. -->
                <StackPanel Margin="0,0,0,24">
                    <TextBlock Text="{Binding Title}" Style="{StaticResource Text.H1}" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding Summary}" Style="{StaticResource Text.Body.Secondary}" TextWrapping="Wrap"/>
                </StackPanel>

                <!-- Features card. -->
                <Border Style="{StaticResource Card.Default}" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Dịch vụ cung cấp" Style="{StaticResource Text.H3}" Margin="0,0,0,14"/>
                        <ItemsControl ItemsSource="{Binding Capabilities}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="0,4">
                                        <TextBlock Text="✦" Foreground="{DynamicResource Brush.Brand.Accent}" Margin="0,0,10,0"/>
                                        <TextBlock Text="{Binding}" Style="{StaticResource Text.Body}" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Important note card. -->
                <Border Style="{StaticResource Card.Info}" BorderBrush="{DynamicResource Brush.Status.Warning}" BorderThickness="4,1,1,1" Margin="0,0,0,20">
                    <TextBlock TextWrapping="Wrap" Style="{StaticResource Text.Body.Secondary}">
                        <Run Text="⚠ Lưu ý: " FontWeight="SemiBold" Foreground="{DynamicResource Brush.Status.Warning}"/>
                        <Run Text="Ở chức năng Chuẩn hóa, phần mềm nhận dạng ký hiệu A. / B. / C. / D. là đáp án lựa chọn."/>
                    </TextBlock>
                </Border>

                <!-- Contact buttons section. -->
                <Border Style="{StaticResource Card.Default}" Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="Thông tin liên hệ" Style="{StaticResource Text.H3}" Margin="0,0,0,14"/>
                        <ItemsControl ItemsSource="{Binding ContactItems}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Label}" Style="{StaticResource Button.Ghost}" Margin="0,0,10,10" Padding="14,8"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Footer message card. -->
                <Border Style="{StaticResource Card.Accent}">
                    <TextBlock TextWrapping="Wrap" Style="{StaticResource Text.Body}">
                        <Run Text="💙 "/>
                        <Run Text="Nếu có góp ý, vui lòng liên hệ nhóm hỗ trợ để ezMix hoàn thiện hơn."/>
                    </TextBlock>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
